// ==========================
// Fonction pour définir le titre de la page
// ==========================

export const setPageTitle = newPageTitle => {
  // Met à jour le titre du document HTML
  // Cette modification est visible dans l'onglet du navigateur
  document.title = newPageTitle;
};
